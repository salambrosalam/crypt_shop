
const initialState = {
    login: null,
    password: null,
    id: null,
    error: null,
    isFetching: false,
    isAuthenticated: false
}

const authReducer = (state = initialState, action) => {
    switch (action.type){

    }
}

const loginThunkCreator = (login,password) => dispatch => {

}
